public abstract class Vehicle {

    Driver driver;
  //  Boolean letsGo = false;
  //  int vehicleAge = 18;


    public void drive(){

    }

    public void setDriver(Driver driver) {
       /* if (checkAge(driver.driver, driver.age)) {
            this.driver = driver;
            System.out.println("Driver changed to: " + driver.driver);
            letsGo = true;
        }*/
    }

 /*   private boolean checkAge(String name, int age){
        if (age < vehicleAge){
            System.out.println("Driver not changed! " + name + " is " + age + ", but you must be " + vehicleAge + " or older to drive a car");
        }else {
            return true;
        }
        return false;
    }*/

}
